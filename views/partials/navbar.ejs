<nav class="navbar bg-light navbar-expand-lg navbar-light">
<div class="container-fluid">
  <% let d = new Date(); %>
  <% let str =  (d.getFullYear() + "-" ) %>
  <% if(d.getMonth() + 1 < 10){str+= 0;}%>
  <% str+= (d.getMonth() + 1) + "-"; %>
  <% if(d.getDate() < 10){ str += 0;} %>
  <% str += d.getDate();%>
  <% let today = new Date(str)%>
  <a class="navbar-brand" href="/">TaskUp</a>
  <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link active" aria-current="page" href="/">Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/today"> Today's Task Schedule</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/assignmentCreate"> Add Assignment</a>
      </li>

      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          Current Assignment Pages
        </a>
        <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
             <% for(let i in allAss){ %>
               <% let name = allAss[i]["name"] + " for " + allAss[i]["subject"] %>
               <% let ref = "/assignment/" + allAss[i]["id"] %>
               <% let due = new Date(allAss[i]["due"])%>
               <% if (today <=due){ //check if the assignment wasn't due before today%>
                 <li><a class="dropdown-item" href=<%=ref%> > <%=name%> </a></li>
                 <%}}%>
        </ul>
      </li>


      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          Past Assignment Pages
        </a>
        <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
             <% for(let i in allAss){ %>
               <% let name = allAss[i]["name"] + " for " + allAss[i]["subject"] %>
               <% let ref = "/assignment/" + allAss[i]["id"] %>
               <% let due = new Date(allAss[i]["due"])%>
               <% if (today > due){ //check if the assignment wasn't due before today%>
                 <li><a class="dropdown-item" href=<%=ref%> > <%=name%></a></li>
                 <%}}%>
        </ul>
      </li>



    </ul>
  </div>
</div>
</nav>
